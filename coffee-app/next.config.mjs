/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // basePath: '/Coffee-Landing-main', // أزل التعليق عن هذا السطر إذا كنت سترفع المشروع على GitHub Pages
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
